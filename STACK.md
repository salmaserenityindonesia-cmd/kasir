# Technology Stack

> Generated by /codebase-mapper on 2026-09-12

## Runtime
| Technology | Version | Purpose |
|------------|---------|---------|
| PHP        | 8.x     | Primary server-side programming language. |
| Node.js    | >= 14   | Used for development builds and asset compilation (implied by `package.json` in dependencies). |

## Application Framework
| Package          | Version | Purpose |
|------------------|---------|---------|
| CodeIgniter      | 4.x     | Core MVC framework providing routing, database ORM, and MVC structure. |

## Production Dependencies
*(Note: Full dependencies are managed via Composer and typically vendored. Key observed packages include:)*
| Package          | Purpose |
|------------------|---------|
| Open Source POS  | Core point-of-sale business logic. |
| Bootswatch       | CSS framework themes (e.g., Yeti, Superhero, Flatly) used in `public/resources/bootswatch5/`. |
| jQuery           | DOM manipulation and AJAX requests (located in `public/resources/js/`). |
| Bootstrap        | UI framework (v4/v5, indicated by resources). |
| DOMPDF           | PDF generation for reports and receipts (found in vendor). |

## Development Dependencies
| Package          | Purpose |
|------------------|---------|
| PHPUnit          | Automated testing framework (`vendor/phpunit/phpunit`). |
| PHPStan          | Static analysis tool for finding bugs (`vendor/phpstan/phpstan`). |
| Rector           | Automated refactoring tool (`vendor/rector/rector`). |
| PHP-CS-Fixer     | Code style enforcement (`vendor/friendsofphp/php-cs-fixer`). |

## Infrastructure
| Service | Provider | Purpose |
|---------|----------|---------|
| MySQL   | Docker   | Relational database for storing all application data. |
| Web     | Docker   | Nginx / Apache web server container (via `docker-compose.yml`). |

## Configuration
| Variable | Purpose | Required |
|----------|---------|----------|
| `CI_ENVIRONMENT` | Sets the application environment (e.g., `production`, `development`). | Yes |
| `FORCE_HTTPS`    | Forces HTTPS redirects if set to `true`. | No |
| `PHP_TIMEZONE`   | Sets the default timezone for the PHP environment (e.g., `UTC`). | Yes |
| `MYSQL_HOST_NAME`| Database host address. | Yes |
| `MYSQL_DB_NAME`  | Database schema name (default `ospos`). | Yes |
| `MYSQL_USERNAME` | Database user. | Yes |
| `MYSQL_PASSWORD` | Database password. | Yes |
